<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="@dimen/fragment_movies_grid_recycler_picture_width"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/spacing_normal"
    android:layout_marginStart="@dimen/spacing_normal">
    <androidx.cardview.widget.CardView
        android:id="@+id/recycler_item_movies_grid_card"
        android:layout_width="match_parent"
        android:layout_height="@dimen/fragment_movies_grid_recycler_picture_height"
        android:elevation="@dimen/elevation_normal"
        app:cardCornerRadius="@dimen/corner_radius_normal"
        app:cardUseCompatPadding="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent">
        <ImageView
            android:id="@+id/recycler_item_movies_grid_card_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/sample_cover"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"/>
    </androidx.cardview.widget.CardView>
    <TextView
        android:id="@+id/recycler_item_movies_grid_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/recycler_item_movies_grid_card"
        android:textSize="@dimen/text_size_small"
        android:textAlignment="center"
        android:text="This is a sample title!"
        android:maxLines="2"/>

</androidx.constraintlayout.widget.ConstraintLayout>